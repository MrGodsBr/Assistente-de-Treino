{
  "dependencies": {
    "node-fetch": "^3.0.0",
    "oauth-1.0a": "^2.2.6" 
    // ^ Use um pacote de autenticação OAUTH para assinar as requisições FatSecret
    // Este pacote deve ser instalado para que a Vercel Function possa utilizá-lo.
  }
}
